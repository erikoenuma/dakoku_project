<%# エラーメッセージ（bootstrap適用） %>

<% if object.errors.any? %>
  <div class="alert alert-danger" role="alert">
  <div>
    <ul>
      <% object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
  </div>
<% end %>