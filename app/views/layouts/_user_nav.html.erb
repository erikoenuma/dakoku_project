<%# 個人ログイン時 navigation %>

<ul class="navbar-nav ml-auto">
    <li class="nav-item">
        <%# 打刻 %>
        <% if current_user.user_projects.empty? %>
            <a class="nav-link" href="#">打刻</a>
        <% else %>
            <a class="nav-link" href="/user_projects/<%= current_user.user_projects.first.id %>/attendance_tracks/top" data-turbolinks="false">打刻</a>
        <% end %>
    </li>
    <%# 実績 %>
    <li class="nav-item">
        <a class="nav-link" href="/user_projects/<%= current_user.user_projects.first.id %>/attendance_tracks" data-turbolinks="false">実績</a>
    </li>
    <%# 申請 %>
    <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
        申請
        </a>
    </li>
    <%# 案件一覧 %>
    <li class="nav-item">
        <a class="nav-link" href="/users/<%= current_user.id %>/projects">案件一覧</a>
    </li>
    <%# アカウント %>
    <li class="nav-item">
        <a class="nav-link" href="/users/<%= current_user.id %>">アカウント</a>
    </li>
</ul>