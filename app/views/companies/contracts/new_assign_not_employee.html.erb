<h1><%= t('.title') %></h1>
<p id="alert"><%= alert %></p>

<%= form_with(model: @contract, url: create_not_employee_assignment_company_contracts_url(@company, @project), local: true) do |form| %>
  <% if @contract.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contract.errors.count, "error") %> prohibited this @contract from being saved:</h2>

      <ul>
        <% @contract.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label "メールアドレス" %>
    <%= form.email_field :email, required :true, maxlength:"255" %>
  </div>

  <div class="field">
    <%= form.label :wage %>
    <%= form.number_field :wage %>円/<%= form.select :wage_per, [["月", "月"], ["1時間","1時間"]] %>
  </div>

  <div class="field">
    <%= form.label :hours_per_month %>
    <%= form.number_field :hours_per_month %>時間
  </div>

  <div class="field">
    <%= form.label :start_at %>
    <%= form.date_select :start_at, required :true %>
  </div>

  <div class="field">
    <%= form.label :end_at %>
    <%= form.date_select :end_at, include_blank:"" %>
  </div>

  <div class="field">
    <%= form.label :role %>
    <%= form.text_field :role, required :true, maxlength:"255" %>
  </div>

  <div class="field">
    <%= form.label :daily_reports_required %>
    <%= form.radio_button :daily_reports_required, true %>要
    <%= form.radio_button :daily_reports_required, false %>不要
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
