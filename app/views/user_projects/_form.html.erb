<%= form_with(model: user_project, local: true) do |form| %>
  <% if user_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
        <% project.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project_id, "案件名" %>
    <%= form.collection_select :project_id, @company.projects.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :employee_type, "雇用形態" %>
    <%= form.radio_button :employee_type, "社員" %>
    <%= form.radio_button :employee_type, "社員以外" %>
  </div>

  <div class="field">
    <%= form.label :email, "メールアドレス" %>
    <%= form.email_field :email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
