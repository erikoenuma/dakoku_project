<h1><%= t('.title') %></h1>

<%= form_with(model: @attendance_track, url: user_project_attendance_tracks_path(@user_project), local: true) do |form| %>

  <div class="form-group">
    <%= form.label :start_at %>
    <%= form.time_select :start_at, required: true %>
  </div>

  <div class="form-group">
    <%= form.label :end_at %>
    <%= form.time_select :end_at, include_blank: "" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>


<%= link_to t('helpers.links.back'), :back %>
